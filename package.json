{
  "name": "poolzGovernor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-watch": "nodemon --watch 'contracts/**/*.sol' --watch 'test/**/*.ts' --ext 'sol,ts' --exec 'npx hardhat test --typecheck'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.14.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.1",
    "@poolzfinance/vault-manager": "^1.0.0"
  }
}
